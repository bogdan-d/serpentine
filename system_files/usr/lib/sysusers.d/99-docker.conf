# sysusers.d vendor entry for docker group
# Packages typically install to /usr/lib/sysusers.d; include a vendor file to match expectations
# Format per systemd: 'g NAME ID' — use '-' for automatic GID allocation
g docker -
