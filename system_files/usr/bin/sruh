#!/bin/bash

# Check for special case "bruh moment"
if [[ "$1" == "moment" ]]; then
  exec serpentine-rollback-helper rollback "$@"
  exit
fi

# Otherwise, pass all arguments to serpentine-rollback-helper
exec serpentine-rollback-helper "$@"
