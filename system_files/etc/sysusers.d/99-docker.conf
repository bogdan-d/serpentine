# sysusers.d entry for docker group
# This ensures system tools that check sysusers.d (like bootc lint) know the docker group
# Format per systemd: 'g NAME ID' — use '-' for automatic GID allocation
g docker -
